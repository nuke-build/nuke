version: "1.0"

linter: jetbrains/qodana-dotnet:latest
failThreshold: 0

dotnet:
  solution: nuke-common.sln

exclude:
  - name: CheckNamespace
  - name: All
    paths:
      - source/Nuke.Build/Execution/Extensions/execution-plan.html
