# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [AzurePipelines (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration AzurePipelines --host AzurePipelines
#
# </auto-generated>
# ------------------------------------------------------------------------------

stages:
  - stage: ubuntu_latest
    displayName: 'ubuntu-latest'
    dependsOn: [  ]
    pool:
      vmImage: 'ubuntu-latest'
    jobs:
      - job: Test
        displayName: '🚦 Test 🧩'
        dependsOn: [  ]
        strategy:
          parallel: 2
        steps:
          - task: Cache@2
            inputs:
              key: $(Agent.OS) | global.json, source/**/*.csproj
              restoreKeys: $(Agent.OS)
              path: ~/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './build.cmd Restore Compile Test --skip --test-partition $(System.JobPositionInPhase)'
            env:
              AzurePipelinesAccessToken: $(System.AccessToken)
              EnterpriseAccessToken: $(EnterpriseAccessToken)
              SlackUserAccessToken: $(SlackUserAccessToken)
          - task: PublishBuildArtifacts@1
            inputs:
              artifactName: test-results
              pathtoPublish: 'output/test-results'
      - job: Pack
        displayName: '📦 Pack'
        dependsOn: [  ]
        steps:
          - task: Cache@2
            inputs:
              key: $(Agent.OS) | global.json, source/**/*.csproj
              restoreKeys: $(Agent.OS)
              path: ~/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './build.cmd Restore Compile Pack --skip'
            env:
              AzurePipelinesAccessToken: $(System.AccessToken)
              EnterpriseAccessToken: $(EnterpriseAccessToken)
              SlackUserAccessToken: $(SlackUserAccessToken)
          - task: PublishBuildArtifacts@1
            inputs:
              artifactName: packages
              pathtoPublish: 'output/packages'
  - stage: windows_latest
    displayName: 'windows-latest'
    dependsOn: [  ]
    pool:
      vmImage: 'windows-latest'
    jobs:
      - job: Test
        displayName: '🚦 Test 🧩'
        dependsOn: [  ]
        strategy:
          parallel: 2
        steps:
          - task: Cache@2
            inputs:
              key: $(Agent.OS) | global.json, source/**/*.csproj
              restoreKeys: $(Agent.OS)
              path: ~/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './build.cmd Restore Compile Test --skip --test-partition $(System.JobPositionInPhase)'
            env:
              AzurePipelinesAccessToken: $(System.AccessToken)
              EnterpriseAccessToken: $(EnterpriseAccessToken)
              SlackUserAccessToken: $(SlackUserAccessToken)
          - task: PublishBuildArtifacts@1
            inputs:
              artifactName: test-results
              pathtoPublish: 'output/test-results'
      - job: Pack
        displayName: '📦 Pack'
        dependsOn: [  ]
        steps:
          - task: Cache@2
            inputs:
              key: $(Agent.OS) | global.json, source/**/*.csproj
              restoreKeys: $(Agent.OS)
              path: ~/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './build.cmd Restore Compile Pack --skip'
            env:
              AzurePipelinesAccessToken: $(System.AccessToken)
              EnterpriseAccessToken: $(EnterpriseAccessToken)
              SlackUserAccessToken: $(SlackUserAccessToken)
          - task: PublishBuildArtifacts@1
            inputs:
              artifactName: packages
              pathtoPublish: 'output/packages'
  - stage: macOS_latest
    displayName: 'macOS-latest'
    dependsOn: [  ]
    pool:
      vmImage: 'macOS-latest'
    jobs:
      - job: Test
        displayName: '🚦 Test 🧩'
        dependsOn: [  ]
        strategy:
          parallel: 2
        steps:
          - task: Cache@2
            inputs:
              key: $(Agent.OS) | global.json, source/**/*.csproj
              restoreKeys: $(Agent.OS)
              path: ~/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './build.cmd Restore Compile Test --skip --test-partition $(System.JobPositionInPhase)'
            env:
              AzurePipelinesAccessToken: $(System.AccessToken)
              EnterpriseAccessToken: $(EnterpriseAccessToken)
              SlackUserAccessToken: $(SlackUserAccessToken)
          - task: PublishBuildArtifacts@1
            inputs:
              artifactName: test-results
              pathtoPublish: 'output/test-results'
      - job: Pack
        displayName: '📦 Pack'
        dependsOn: [  ]
        steps:
          - task: Cache@2
            inputs:
              key: $(Agent.OS) | global.json, source/**/*.csproj
              restoreKeys: $(Agent.OS)
              path: ~/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './build.cmd Restore Compile Pack --skip'
            env:
              AzurePipelinesAccessToken: $(System.AccessToken)
              EnterpriseAccessToken: $(EnterpriseAccessToken)
              SlackUserAccessToken: $(SlackUserAccessToken)
          - task: PublishBuildArtifacts@1
            inputs:
              artifactName: packages
              pathtoPublish: 'output/packages'
